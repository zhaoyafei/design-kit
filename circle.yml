general:
  build_dir: .
  artifacts:
    - "css" # Compiled adk.css file

dependencies:
  pre:
    - npm install -g bower gulp
  override:
    - bower install
    - npm install
    - gulp sass
    - gulp sherpa

test:
  override:
    - echo 'No Tests'

deployment:
  design-kit.arc.ht:
    owner: Architizer
    branch: master
    commands:
      - bash -x deploy.sh

  development-builds:
    owner: Architizer
    branch: /.*?/
    commands:
      - bash -x deploy.sh

experimental:
  notify:
    branches:
      only:
        - master